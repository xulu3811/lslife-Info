import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { await prisma.post.updateMany({ where: { status: 'published' }, data: { status: 'PUBLISHED' } }); await prisma.post.updateMany({ where: { status: 'pending_review' }, data: { status: 'MANUAL_REVIEWING' } }); await prisma.post.updateMany({ where: { status: 'rejected' }, data: { status: 'REJECTED' } }); await prisma.post.updateMany({ where: { status: 'removed' }, data: { status: 'REMOVED' } }); console.log('Migrated'); } main().finally(() => prisma.$disconnect());
